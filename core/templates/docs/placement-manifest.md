---
wo: WO-XXXX
title: "[Platform name] — Documentation Placement Manifest"
version: 1.0
status: DRAFT
created: YYYY-MM-DD
last-modified: YYYY-MM-DD
reviewed-by: N/A
---

# [Platform name] — Documentation Placement Manifest

<!-- Generated content has to land somewhere. This manifest is a deliverable,
     not a note: without it, content sits in a work-order folder and is never
     read.

     Every produced file appears exactly once. Every target path is checked
     for an existing file, and the action says what happens to it. A manifest
     that silently overwrites is how a team loses documentation it wrote by
     hand. -->

---

## Placements

| Produced file | Target path | Action | Replaces | Checked |
|---|---|---|---|---|
|  |  | new / replace / merge |  |  |

---

## Frontmatter Stripped on Publication

<!-- Internal identifiers are internal terminology and must not appear on a
     consumer-facing page. Record which fields are removed from the published
     copy. -->

| Field | Kept in the work-order copy | On the published page |
|---|---|---|
| `wo` | yes | removed |
| `reviewed-by` | yes | removed |

---

## Navigation Proposal

<!-- WRITING PROMPT: Propose the navigation change as a diff against the
     existing structure — MkDocs-style navigation where the docs portal uses
     it, or whatever the project's site consumes.

     Rules regardless of format: at most three levels deep; sections named for
     tasks rather than internal modules; the quickstart reachable first; no
     orphan pages; every cross-link relative and resolving to a page that
     exists. -->

```
[the proposed navigation, as a diff or an annotated tree]
```

---

## Link Check

| Cross-link | Target exists | Relative |
|---|---|---|
|  |  |  |

---

## Applied

<!-- The docs owner applies this manifest; the generator never applies it
     silently. -->

| Date | Applied by | Notes |
|---|---|---|
|  |  |  |
