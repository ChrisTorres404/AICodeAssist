# install-experience

Proves the four gaps a mature-codebase run exposed: the self-test skips installer tests in an installed copy instead of failing them, doctor proves a full lifecycle in this layout rather than trusting it, a commit citing a work order that does not exist is reported (and blocked in strict), and a verification still full of template placeholders cannot be closed.
