# install-self-guard

Proves that running install.sh from the installed copy inside a project refuses (or re-runs from the recorded source) instead of deleting the installed pipeline, that wo suite scaffolds a suite that exits non-zero on failure, and that detect-stack --write installs the rule sets the code needs.
