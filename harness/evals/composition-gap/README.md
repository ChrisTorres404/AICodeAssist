# composition-gap

Proves the gap that the parallel build exposed: two modules whose own suites both pass while the composition is broken. The integration work order runs the covered suites and the cross-module walk, and fails until the composition is fixed.
