# hooks-merge

Proves pipeline hooks are merged by identity: a user's own hook survives installation, reinstalling adds no duplicates, a tampered pipeline hook is restored, and the minimal profile removes only pipeline hooks.
